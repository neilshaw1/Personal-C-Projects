#include<iostream>
#include<math.h>
using namespace std;

int main()
{
int sideLength;
sideLength=7;
cout<<"The volume of a cube with a side length of "<<(sideLength)<<" is "<<(pow(sideLength, 3))<<".";
return 0;
}
